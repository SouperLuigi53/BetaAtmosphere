/** Test-only texture base; lets the original Beta bytecode run without OpenGL. */
public class aw {
    public byte[] a = new byte[1024];
    public boolean c;
    public int e;
    public aw(int ignoredTextureIndex) {}
}
